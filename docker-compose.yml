version: '3.8'
services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
  runtime:
    build: ./backend/runtime
    ports:
      - "4005:4005"
  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
